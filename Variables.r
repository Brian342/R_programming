# Variables are used to store data with named locations 
# that your programs can manipulate. a variable name can be a 
# combination of letters,digits,periods and underscore
model1 <-'hello'
typeof(model1)
class(model1)
.pairs <- 10
.pairs
hellostring <- 'real hello'
hellostring
# no spacing between the variable name
