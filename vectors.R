# Vector is a linear which contains homogenous elements e.g c(1,2,3), c(TRUE, FALSE)
# creating vector with more than one element, use c() function which means to combine the elements into a vector
# Create a vector
  apples <- c('red','green',"yellow")
  print(apples)

